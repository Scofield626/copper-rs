# cu-anytime-rrt-star

Example Copper app demonstrating the `Anytime` trait / `AnytimeTask<A>` adapter
via an incremental RRT\* motion planner.

Scaffold only: the planner, occupancy grid, and end-to-end graph are wired in
follow-up commits (`feat/anytime-rrt-star`).
